# AuditSphereOps — implementation checklist

Dependency-ordered per spec Appendix G and the "first proving slice". One item at a time: build + PostgreSQL-backed tests + owner-authorized review/commit before moving on. The agent leaves changes uncommitted unless explicitly asked to commit. Statuses: ✅ done · 🟡 partial · ⬜ not started · 🚫 blocked (external).

| # | Item | Spec ref | Acceptance evidence required | Status |
|---|------|----------|------------------------------|--------|
| 1 | Toolchain pinned, 6-project solution, PG 18.6, runnable bootstrap | ND-01, A01/B01 | Clean build; tests run on real PG | ✅ `671d349` |
| 2 | Accounting persistence integrity: composite scope FKs, append-only TB rows, posted-journal line immutability, single-claim worker guard | ND-03, VT | FK violation rejected; row UPDATE/DELETE rejected; concurrent workers never double-process | ✅ |
| 3 | Durable outbox: claim/lease/attempt fence/idempotency-key uniqueness | ND-03, ADR-06, §29 | Two workers claim distinct ops; retry does not duplicate side effects | ✅ `cc6384c` pushed to `origin/master`; provider simulation only |
| 4 | Synthetic client/identity + authorization decision enforced inside commands | ND-02, A02/B02, §8 | Cross-firm/client access denied by command, not only UI | ✅ `16f10f4` (local verification; Entra/tenant proof still blocked) |
| 5 | TB import (CSV) → validate → AJ post → re-upload source bridge | NT-09/10, AT-07/10 | Reflected journal never re-applied; profit stays 175,000 | ✅ `995990e` (local verification; mapping/FS package still open) |
| 6 | Practice CRM commands (lead→opportunity→proposal→client) | ND-05, AT-01–06 | Commercial/professional separation enforced | ✅ `141a55d`, PR #1 merged as `881eda1` (local verification; no independent human review) |
| 7 | Time/budget approval + correction chain | ND-06 | Corrections supersede, never rewrite approved time | ✅ `0a34400`, PR #2 merged as `b1b23be` (local verification; no independent human review) |
| 8 | Billing artifacts (invoice→receipt→allocation) | ND-07, §41 | Balanced expected fixtures; no invoice-history rewrite | ✅ `88e3409`, PR #3 merged as `3bcdaa7` (local verification; no independent human review) |
| 9 | Firm ledger (accounts, periods, draft→posted immutable, source uniqueness) | ND-08, NT-22 | Period race rejected; posted journals immutable | ✅ `ea40814`, PR #4 merged as `c01f3d3` (local verification; no independent human review) |
| 10 | Document snapshots (exact-byte hash, unique version per doc) | ND-04, §11 | Duplicate snapshot identity rejected; hash verified | ✅ `0f9fe20`, PR #5 merged as `c091bf2` (local verification; no independent human review) |
| 11 | Approvals bound to revision + generation; stale rejected | §22, AT-19/26 | Change after approval invalidates it; stale approval fails release | ✅ `94a8da3`, PR #6 merged as `87e2523` (local verification; no independent human review) |
| 12 | Release gate (manifest digest, external checkpoint) | §24, AT-25/26 | Wrong-version report blocked; no checkpoint, no release | ✅ `3e3c82a`, PR #7 merged as `2dd97f9` (local verification; no independent human review) |
| 13 | Blazor staff/portal screens over implemented commands | ND-10, §43 | Real screens driving real commands | 🟡 `c127448`, PR #8 merged as `ff57783` (initial shell only; remaining UI open) |
| 14 | CI, backup/restore drill, readiness with migration check | ND-11, §45 | Pipeline green; restore rehearsal documented | 🟡 `ab8e1d1` (local verification; hosted CI pending) |
| 15 | Entra OIDC, SharePoint grants, Purview profile, methodology/signing approvals, independent review + protected merge | §47.2 | Owner-supplied tenant/professional evidence | 🚫 |
| 16 | Full-cycle acceptance run on real tenant | §47, §1.4 | All 120 scenarios observed as specified | 🚫 |

## Item log

- **#1 — done** (commit `671d349`, 2026-09-17): six net10.0 projects; EF Core 10.0.12/Npgsql 10; PostgreSQL 18.6 user-local; `scripts/db` operational; 6/6 tests green.
- **#2 — done** (2026-09-17): migration `20260917091412_AccountingIntegrity` adds composite scope FKs (dataset→engagement/client within the same firm, rows→dataset, journals→dataset/engagement, lines→journal), an append-only trigger on `trial_balance_rows` (UPDATE/DELETE rejected — corrections mean a new dataset), and a freeze trigger on `adjustment_lines` once the journal leaves Draft. 6 new PostgreSQL tests: orphan row FK, unknown engagement FK, cross-firm client FK, append-only rows, frozen journal lines, and a two-worker concurrency test proving one claim per dataset. **Bug found by tests and fixed:** the first trigger version returned OLD on UPDATE, silently discarding Draft-phase line edits.
- **#3 — done** (commit `cc6384c`, 2026-09-18, pushed to `origin/master`): migration `20260917104422_DurableOutbox`; one durable operation state machine, scoped payload-bound idempotency, atomic SKIP LOCKED claims, renewable leases, monotonic attempt fences, append-only evidence, and safe uncertain-result reconciliation. Trial-balance validation now uses the typed local handler without changing its accounting outcomes. Re-verified at HEAD: build 0 warnings/errors; full suite 40/40, 0 skipped. Tests cover simulated provider success followed by local failure, stale completion/renewal, renewal waiting past lease expiry, changed inputs, cancellation, constraints, rollback, and migration refusal with legacy operations. Applied only to local `auditsphere`; real provider/tenant acceptance and independent review remain outstanding.
- **#4 — done** (commit `16f10f4`, 2026-09-18): `AuthorizationDecision.AuthorizeAsync` enforces firm scope, stored-scope linkage, covering assignment, required roles, client internal-only denial, stale/disabled sessions, and hold/professional-work gates inside commands; `TrialBalanceDatasetQuery` resolves scope from the stored row so guessed IDs fail closed. Migration `20260918041428_AuthorizationIntegrity` adds `ux_users_tenant_subject`, user/grant/hold checks, and composite FKs (engagement→client, grants→user/client/engagement, holds→engagement). Full suite 51/51; applied only to local `auditsphere`. Entra OIDC, SharePoint grants, and tenant acceptance remain blocked/external.
- **#5 — done** (commit `995990e`, 2026-09-18): safe CSV importer (string codes, exact decimals ≤6dp, one currency, formula/NaN/duplicate rejection, 20k-row cap); guarded import command with source-hash duplicate reuse and per-engagement revisions; AJ draft/post with reviewer separation of duties and posted-line freeze; reviewer source bridge (evidence-required reflection) plus immutable plans that apply only NOT_REFLECTED journals and block UNKNOWN/PARTIALLY/stale decisions. Full cycle on Appendix D: TB-1 + AJ-001 → profit 175,000; replacement TB-2 marked REFLECTED → plan applies zero, profit stays 175,000. Migration `20260918083524_AdjustmentSourceBridge` (3 tables, partial duplicate-hash index, `created_by_user_id`). Full suite 62/62; applied only to local `auditsphere`.
- **#6 — implemented and merged** (commit `141a55d`, 2026-09-18, PR #1 merged as `881eda1`): guarded commercial commands for lead qualification, opportunity progression, proposal revision/approval/send/response, idempotent conversion to a canonical `PracticeClient` draft, and scoped client-contact creation. Lead creation uses the firm guard for exact normalized duplicate rejection, and every CRM state transition requires the guard in a local transaction. Conversion provisions `ClientSafetyState`, locks/increments the existing client generation, binds the pending `AcceptanceDecision` to that generation, and never creates or activates an engagement; sent proposal versions are retained and stale revisions are rejected. Migrations `20260918093347_PracticeCrmWorkflow` and `20260918095500_CrmSafetyInvariants` are applied only to local `auditsphere`; owner IDs and client contacts are firm-scoped, contact mutations increment the client generation, primary contacts serialize under the client guard, malformed contact data is rejected, and invalid ranges/state values are database-rejected. Full suite 69/69; 0 skipped. GitGuardian passed; the automated reviewer was neutral and no independent human review was observed.
- **#7 — implemented and merged** (commit `0a34400`, 2026-09-18, PR #2 merged as `b1b23be`): guarded task assignment/completion, integer-minute time drafts, bounded overlap checks, submit/independent approval, immutable rate snapshots, correction revisions that supersede approved rows, versioned rate cards and engagement budgets, stale-version fencing, approval segregation, and budget-versus-approved-time actuals. Migration `20260918111815_PracticeTimeBudgetWorkflow` preserves the legacy budget table under `legacy_budget_versions`, converts only minute-aligned legacy hours, refuses orphan/ambiguous legacy rows, and adds scoped FKs/checks. Full suite 74/74; 0 skipped. GitGuardian passed; no independent human review was recorded.
- **#8 — implemented and merged** (implementation commit `88e3409`, 2026-09-18, PR #3 merged as `3bcdaa7`): guarded billing-account creation, invoice draft/review/approval/post/send states, owner-approved finance-profile gate, immutable credit notes, receipt recording/allocation balance checks, scoped finance-role authorization, and unique source-allocation fencing for time/milestone billing. Migration `20260918115026_BillingArtifactWorkflow` preserves legacy allocation rows, normalizes only unambiguous legacy invoice/receipt state, adds scoped FKs/checks and six append-only history triggers, and refuses ambiguous legacy billing data. Full suite 76/76; 0 skipped. No independent human review was recorded.
- **#9 — implemented and merged** (implementation commit `ea40814`, 2026-09-18, PR #4 merged as `c01f3d3`): bounded firm accounts and periods, finance-role journal draft/review/approval/post transitions, exact balanced immutable postings, source/retry uniqueness, reversal postings, period close/reopen decisions, posting-account gates, and firm/period lock ordering. Migration `20260918121512_FirmLedgerWorkflow` adds scoped keys/FKs/checks, immutable journal/posting/source/receipt/decision triggers, a deferred multi-row balance trigger, and a safe refusal for ambiguous legacy account/journal rows. Ledger tests cover 4 workflow/security/database cases plus the legacy migration refusal; full suite 81/81, 0 skipped. Local `auditsphere` has all eleven migrations applied; no production database or external provider was touched. No independent human review was recorded.
- **#10 — implemented and merged** (implementation commit `0f9fe20`, 2026-09-18, PR #5 merged as `c091bf2`): `DocumentSnapshotService` authorizes the stored document scope, hashes the supplied bytes internally with SHA-256, records byte count/source identity, rejects duplicate `(firm, document, provider-version)` identities, and returns no provider success claim. Migration `20260918125731_DocumentSnapshotIntegrity` adds scope FKs, bounded hash/version/byte checks, unique version identity, an append-only snapshot trigger, and legacy preflight that refuses ambiguous scope or hashes. Three snapshot integrity tests plus the migration suite pass; full suite 85/85, 0 skipped. Local `auditsphere` has all twelve migrations applied; no production database or external provider was touched. No independent human review was recorded.
- **#11 — implemented and merged** (implementation commit `94a8da3`, 2026-09-18, PR #6 merged as `87e2523`): `ApprovalService` binds a stored workpaper approval to its current target revision, client input generation, firm policy generation and manifest digest; it writes immutable historical decisions plus a separately updated applicability projection. Current evaluation re-reads locked guard/target state with no-tracking reads, marks changed input/policy/revision `STALE`, and blocks `RequireCurrentAsync`. Migration `20260918131715_ApprovalApplicabilityWorkflow` adds policy-generation storage, scope FKs/checks, approval identity uniqueness, legacy approval refusal, and an append-only approval trigger. Two approval tests plus the migration suite pass; full suite 88/88, 0 skipped. Local `auditsphere` has all thirteen migrations applied; no production database or external provider was touched. No independent human review was recorded.
- **#12 — implemented and merged** (commit `3e3c82a`, 2026-09-18, PR #7 merged as `2dd97f9`): `ReleaseService` creates a workpaper release candidate bound to an approved target revision, client input generation, firm policy generation and manifest digest, then issues one immutable release event only when the current approval remains applicable, professional work is unblocked, the manifest matches, and an external checkpoint is verified. Authorized release keys are firm-scoped and idempotent; issuance writes a durable delivery outbox intent in the same transaction. `20260918133707_ReleaseGateWorkflow` adds scoped candidate/release FKs and checks, legacy release refusal, and an append-only release trigger. Two release tests plus migration safety pass; full suite 91/91, 0 skipped. Local `auditsphere` has all fourteen migrations applied. Signing/protection attestation and live provider delivery remain intentionally outside this local slice; no production database or external provider was touched. Independent human review is still outstanding.
- **#13 — initial UI shell implemented and merged** (commit `c127448`, 2026-09-18, PR #8 merged as `ff57783`): the Web host now serves a Blazor Interactive Server shell with accessible navigation, live PostgreSQL portfolio metrics and scoped release-candidate projections, plus `/app/releases/{id}` invoking the real `ReleaseService.IssueAsync` command. Authenticated users are mapped by trusted Entra subject + tenant to an active stored user and firm; no demo actor or browser-supplied scope exists. With OIDC unset, `/app` is explicitly locked. Smoke evidence: root, liveness, readiness and `/app` returned HTTP 200; full suite 91/91, 0 skipped. The complete route catalog, client portal, upload transport, and recovery/operator screens remain open. Independent human review is still outstanding.
- **#14 — local CI/restore/readiness controls implemented** (clean commit `ab8e1d1`, 2026-09-18, PR pending): the workflow provisions PostgreSQL 18, restores/builds/tests, applies the explicit EF migration step to the disposable database, and checks `/health/ready`. Readiness now includes a migration-history check and fails unhealthy when the schema is not current. `scripts/db/restore-drill.sh` performs a loopback-only custom-format dump/restore into a generated temporary database and verifies all fourteen migrations, then cleans only its generated database and temporary files. Local evidence: build clean, full suite 91/91, restore rehearsal passed, `/health/ready` returned `Healthy`/HTTP 200 with the current schema. The superseded hosted run could not start because the GitHub account Actions billing/spending limit failed; independent human review is still outstanding.
- **Tenant evidence update (2026-09-18):** the requested account authenticated to the easyguide tenant; the Graph identity check passed, broad site enumeration was denied with HTTP 403, existing AuditSphere SharePoint test sites were inspected read-only, Purview was reachable, and no AuditSphere app/grant or tenant-wide consent was created. The records profile, selected-resource grant, professional approvals, and production proof remain absent.
- **Next permitted step:** open the owner-authorized PR for checklist #14, observe hosted CI, then finish the remaining UI surface. No production deployment, app registration, permission grant, secret, certificate, or live-provider action was performed.
