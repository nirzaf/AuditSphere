# AuditSphereOps agent instructions (short pointer; full contract lives in the specification)

- Spec: `AuditSphereOps_NET_Codex_Implementation_Specification.md` at repo root (authoritative v5.0; mirrors `docs/specification/AuditSphereOps.NET.md`). Read intro + §§1–12, 22, 24, 27–33, 41–47 first; then only the sections/tests for the active issue. Status file `docs/execution/status.json` is a pointer — re-read repo/GitHub reality on resume.
- Stack: .NET 10 SDK (`C:\Users\DELL\.dotnet\dotnet.exe`), ASP.NET Core Blazor Web App Interactive Server, EF Core 10 + Npgsql 10, PostgreSQL 18.6 (user-local binaries in `C:\Users\DELL\.pgsql18`), Docker-free local dev.
- Database scripts (development only; loopback + trust auth): `scripts\db\status.ps1`, `start.ps1`, `stop.ps1`, `init-cluster.ps1`. Run with `powershell -NoProfile -ExecutionPolicy Bypass -File <script>`; execution policy blocks unsigned scripts on this workstation, so the bypass flag is required per invocation.
- Never: Frappe/ERPNext/Python backend/React frontend/second ERP; K8s/microservices-per-module/broker/app-AI; tenant-wide Graph scopes; autonomous professional conclusions; destructive repo rewrite; paste spec into this file.
- Env toggles: `set PGSQL_HOME=C:\Users\DELL\.pgsql18\pgsql` (optional override), `set PATH=%PGSQL_HOME%\bin;%PATH%`; no admin needed. `ExternalEffects.Enabled=false` + `AllowSimulationAdapters=true` locally; production requires Entra tenant, selected SharePoint grants, Purview profile, secrets — record as blocked, never fake pass.
- Per change: smallest coherent slice, guarded transaction, scope-checked authz, no stale approvals; `dotnet build` + targeted `dotnet test` (postgres profile when DB touched); update `docs/execution/status.json` + `docs/execution/current-slice.md` only with observed facts.
